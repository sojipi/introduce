# 技术展示后台管理系统

基于阿里云边缘存储的现代化后台管理系统，用于管理个人技术作品集的项目、获奖、技能和时间线数据。

## 🚀 功能特色

### 核心功能
- **项目管理**: 创建、编辑、删除技术项目，支持分类管理和技术栈标记
- **获奖管理**: 记录竞赛获奖情况，支持多种奖项类型和级别
- **技能管理**: 管理技术技能，支持等级评估和可视化展示
- **时间线管理**: 管理参赛和活动日程，支持状态跟踪
- **存储管理**: 直接管理阿里云边缘存储数据，支持备份恢复

### 技术特色
- **边缘存储**: 基于阿里云边缘存储API，数据分布式存储
- **现代前端**: Vue 3 + Element Plus 响应式界面
- **RESTful API**: 标准化API设计，支持CRUD操作
- **数据可视化**: Chart.js图表展示统计数据
- **批量操作**: 支持数据的批量导入导出

## 🛠️ 技术栈

### 后端技术
- **Node.js**: 服务器运行环境
- **Express**: Web应用框架
- **阿里云边缘存储**: 数据存储服务
- **JWT**: 身份认证
- **bcryptjs**: 密码加密

### 前端技术
- **Vue 3**: 渐进式JavaScript框架
- **Element Plus**: Vue 3组件库
- **Vue Router**: 路由管理
- **Axios**: HTTP客户端
- **Chart.js**: 数据可视化

## 📦 安装部署

### 环境要求
- Node.js 16+
- npm 或 yarn
- 阿里云账户（用于边缘存储服务）

### 1. 克隆项目
\`\`\`bash
git clone <repository-url>
cd admin
\`\`\`

### 2. 安装依赖
\`\`\`bash
npm install
\`\`\`

### 3. 环境配置
复制环境变量模板：
\`\`\`bash
cp .env.example .env
\`\`\`

编辑 \`.env\` 文件，配置必要参数：
\`\`\`env
# 服务器配置
PORT=3001
NODE_ENV=development

# JWT密钥
JWT_SECRET=your-super-secret-jwt-key-here

# 阿里云边缘存储配置
ALIYUN_ACCESS_KEY_ID=your-access-key-id
ALIYUN_ACCESS_KEY_SECRET=your-access-key-secret
ALIYUN_ESA_ENDPOINT=https://esa.cn-hangzhou.aliyuncs.com
ALIYUN_ESA_NAMESPACE=tech-showcase
\`\`\`

### 4. 初始化数据
\`\`\`bash
node scripts/init-admin.js
\`\`\`

### 5. 启动服务
开发模式：
\`\`\`bash
npm run dev
\`\`\`

生产模式：
\`\`\`bash
npm start
\`\`\`

## 🔧 阿里云边缘存储配置

### 1. 开通服务
1. 登录阿里云控制台
2. 开通边缘安全加速(ESA)服务
3. 创建边缘存储空间

### 2. 获取访问密钥
1. 访问RAM控制台
2. 创建AccessKey
3. 配置ESA服务权限

### 3. 配置存储空间
1. 在ESA控制台创建NameSpace
2. 记录NameSpace名称
3. 配置访问策略

## 📱 使用指南

### 默认账户
- **用户名**: admin
- **密码**: admin123
- **建议**: 首次登录后立即修改密码

### 主要功能

#### 项目管理
1. 点击"项目管理"进入项目列表
2. 点击"新建项目"创建项目
3. 填写项目信息：名称、描述、分类、技术栈等
4. 支持项目状态管理和批量操作

#### 获奖管理
1. 点击"获奖管理"进入获奖列表
2. 点击"新建获奖记录"添加获奖信息
3. 支持按奖项类型和级别筛选
4. 自动生成获奖统计图表

#### 技能管理
1. 点击"技能管理"进入技能列表
2. 支持技能分类和等级管理
3. 可视化技能等级分布
4. 支持批量导入技能数据

#### 时间线管理
1. 点击"时间线管理"进入事件列表
2. 支持时间线和表格两种视图
3. 事件状态跟踪和筛选
4. 支持事件类型分类

#### 存储管理
1. 点击"存储管理"查看存储统计
2. 浏览和编辑存储数据
3. 数据备份和恢复功能
4. 存储空间清理工具

## 🔒 安全特性

- **JWT认证**: 基于Token的身份验证
- **密码加密**: bcrypt加密存储密码
- **权限控制**: 基于角色的访问控制
- **数据验证**: 前后端双重数据验证
- **CORS配置**: 跨域请求安全控制

## 📊 API文档

### 认证接口
- \`POST /api/auth/login\` - 用户登录
- \`GET /api/auth/me\` - 获取当前用户信息
- \`PUT /api/auth/password\` - 修改密码

### 项目接口
- \`GET /api/projects\` - 获取项目列表
- \`POST /api/projects\` - 创建项目
- \`PUT /api/projects/:id\` - 更新项目
- \`DELETE /api/projects/:id\` - 删除项目

### 获奖接口
- \`GET /api/awards\` - 获取获奖列表
- \`POST /api/awards\` - 创建获奖记录
- \`PUT /api/awards/:id\` - 更新获奖记录
- \`DELETE /api/awards/:id\` - 删除获奖记录

### 技能接口
- \`GET /api/skills\` - 获取技能列表
- \`POST /api/skills\` - 创建技能
- \`PUT /api/skills/:id\` - 更新技能
- \`DELETE /api/skills/:id\` - 删除技能

### 时间线接口
- \`GET /api/timeline\` - 获取时间线事件
- \`POST /api/timeline\` - 创建事件
- \`PUT /api/timeline/:id\` - 更新事件
- \`DELETE /api/timeline/:id\` - 删除事件

### 存储接口
- \`GET /api/storage/stats\` - 获取存储统计
- \`GET /api/storage/keys\` - 获取键列表
- \`GET /api/storage/data/:key\` - 获取数据
- \`PUT /api/storage/data/:key\` - 设置数据
- \`DELETE /api/storage/data/:key\` - 删除数据

## 🚀 部署指南

### Docker部署
\`\`\`bash
# 构建镜像
docker build -t tech-showcase-admin .

# 运行容器
docker run -d -p 3001:3001 --env-file .env tech-showcase-admin
\`\`\`

### PM2部署
\`\`\`bash
# 安装PM2
npm install -g pm2

# 启动应用
pm2 start server.js --name "tech-showcase-admin"

# 查看状态
pm2 status

# 查看日志
pm2 logs tech-showcase-admin
\`\`\`

### Nginx配置
\`\`\`nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
\`\`\`

## 🔧 开发指南

### 项目结构
\`\`\`
admin/
├── server.js              # 服务器入口
├── package.json           # 项目配置
├── .env.example          # 环境变量模板
├── routes/               # API路由
│   ├── auth.js          # 认证路由
│   ├── projects.js      # 项目路由
│   ├── awards.js        # 获奖路由
│   ├── timeline.js      # 时间线路由
│   ├── skills.js        # 技能路由
│   └── storage.js       # 存储路由
├── services/            # 服务层
│   └── edgeStorage.js   # 边缘存储服务
├── scripts/             # 脚本文件
│   └── init-admin.js    # 初始化脚本
└── public/              # 前端文件
    ├── index.html       # 主页面
    ├── css/            # 样式文件
    └── js/             # JavaScript文件
        ├── app.js      # 主应用
        └── components/ # Vue组件
\`\`\`

### 添加新功能
1. 在 \`routes/\` 目录添加新的路由文件
2. 在 \`public/js/components/\` 添加新的Vue组件
3. 在 \`server.js\` 中注册新路由
4. 在 \`public/js/app.js\` 中添加新路由配置

### 数据模型
所有数据存储在阿里云边缘存储中，使用键值对形式：
- 项目: \`project:{id}\`
- 获奖: \`award:{id}\`
- 技能: \`skill:{id}\`
- 时间线: \`timeline:{id}\`
- 用户: \`user:{username}\`
- 列表: \`{type}:list\`

## 🐛 故障排除

### 常见问题

**1. 阿里云边缘存储连接失败**
- 检查AccessKey配置是否正确
- 确认ESA服务已开通
- 验证NameSpace是否存在

**2. 登录失败**
- 运行初始化脚本创建管理员账户
- 检查JWT_SECRET配置
- 确认数据库连接正常

**3. 前端页面无法加载**
- 检查静态文件路径
- 确认端口配置正确
- 查看浏览器控制台错误

**4. API请求失败**
- 检查CORS配置
- 确认API路由注册
- 查看服务器日志

### 日志查看
\`\`\`bash
# 开发模式日志
npm run dev

# PM2日志
pm2 logs tech-showcase-admin

# Docker日志
docker logs <container-id>
\`\`\`

## 📄 许可证

MIT License

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (\`git checkout -b feature/AmazingFeature\`)
3. 提交更改 (\`git commit -m 'Add some AmazingFeature'\`)
4. 推送到分支 (\`git push origin feature/AmazingFeature\`)
5. 打开 Pull Request

## 📞 技术支持

如有问题或建议，请通过以下方式联系：

- 📧 邮箱: support@techshowcase.com
- 🐛 Issues: [GitHub Issues](https://github.com/username/tech-showcase-admin/issues)
- 📖 文档: [在线文档](https://docs.techshowcase.com)

---

**让技术展示更简单，让管理更高效！** 🚀