# 服务器配置
ADMIN_PORT=3001
NODE_ENV=development

# JWT密钥
JWT_SECRET=your-super-secret-jwt-key-here

# KV存储配置
# 选择存储方式: cloud | direct
# cloud: 使用云函数KV存储 (推荐)
# direct: 直接使用阿里云ESA API
KV_STORAGE_MODE=cloud

# 云函数KV配置 (当KV_STORAGE_MODE=cloud时使用)
KV_CLOUD_FUNCTION_URL=https://kv.zenmb.com/kv/
KV_NAMESPACE=intro

# 阿里云边缘存储配置 (当KV_STORAGE_MODE=direct时使用)
ALIYUN_ACCESS_KEY_ID=your-access-key-id
ALIYUN_ACCESS_KEY_SECRET=your-access-key-secret
ALIYUN_ESA_ENDPOINT=https://esa.cn-hangzhou.aliyuncs.com
ALIYUN_ESA_NAMESPACE=tech-showcase

# 其他配置
CORS_ORIGIN=http://localhost:3000